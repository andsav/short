\documentclass[tikz,a4paper,11pt]{article}
\usepackage[T1]{fontenc}
\usepackage[utf8]{inputenc}
\usepackage{graphicx}
\usepackage{xcolor}

\renewcommand\familydefault{\sfdefault}
\usepackage{tgheros}
\usepackage[defaultmono]{droidmono}

\usepackage{amsmath,amssymb,amsthm,textcomp}
\usepackage{enumerate}
\usepackage{multicol}
\usepackage{tikz}
\usepackage[linguistics]{forest}
\usetikzlibrary{backgrounds,fit,positioning}

\usepackage{geometry}
\geometry{total={210mm,297mm},
left=15mm,right=15mm,%
bindingoffset=0mm, top=20mm,bottom=20mm}

\newcommand*{\mysqrt}[4]{\sqrt[\leftroot{#1}\uproot{#2}#3]{#4}}

\linespread{1.3}

\newcommand{\linia}{\rule{\linewidth}{0.5pt}}


\begin{document}

\section*{$T(n) = T(2n/3) + T(n/3) + n^2$}

\begin{center}
\begin{forest} for tree={
    edge path={\noexpand\path[\forestoption{edge}] (\forestOve{\forestove{@parent}}{name}.parent anchor) -- +(0,-12pt)-| (\forestove{name}.child anchor)\forestoption{edge label};}
}
  [$n^2$
    [$\frac{2}{3}n^2$
        [$(\frac{2}{3})^2n^2$
            [$(\frac{2}{3})^3n^2$
                [...]
                [...]
            ]
            [$(\frac{2}{3})^2(\frac{1}{3})n^2$
                [...]
                [...]
            ]
        ]
        [$(\frac{2}{3})(\frac{1}{3})n^2$
            [$(\frac{2}{3})^2(\frac{1}{3})n^2$
                [...]
                [...]
            ]
            [$(\frac{2}{3})(\frac{1}{3})^2n^2$
                [...]
                [...]
            ]
        ]
    ]
    [$\frac{1}{3}n^2$, name=a
        [$(\frac{2}{3})(\frac{1}{3})n^2$
            [$(\frac{2}{3})^2(\frac{1}{3})n^2$
                [...]
                [...]
            ]
            [$(\frac{2}{3})(\frac{1}{3})^2n^2$
                [...]
                [...]
            ]
        ]
        [$(\frac{1}{3})^2n^2$, name=b
            [$(\frac{2}{3})(\frac{1}{3})^2n^2$
                [...]
                [...]
            ]
            [$(\frac{1}{3})^3n^2$, name=c
                [...]
                [...]
            ]
        ]
    ]
  ]
   \node (p) [right=100pt of a] {$ = n^2$};
    \node at (p |- b) {$ = n^2$};
    \node at (p |- c) {$ = n^2$};
\end{forest}
\end{center}
\begin{itemize}
    \item The rightmost branch  - $(\frac{n}{3})^k$ -will reach the bottom of the recursion first
    \item Up until that first branch stops exapnding, the amount of work done on every level sums up to $n^2$
    \item The height of the leftmost branch will be $log_{\frac{3}{2}} n$ while the height of the rightmost branch will be $log_{3}n$
    \item The amount of work between the $(log_{3}n)$\textsuperscript{th} level and the $(log_{\frac{3}{2}}n)$\textsuperscript{th} level will be progressively decreasing from $n^2$ to $1$
    \item Thus we have an upper bound $O(n^2 log_{\frac{3}{2}}n)$ and a lower bound $\Omega(n^2 log_{3}n)$
\end{itemize}

\newpage

\section*{b) $T(n) = \sqrt{n} * T(\sqrt{n}) + n$}

\begin{center}
\begin{forest} 
  [n
    [$\sqrt{n}$
        [$\mysqrt{0}{0}{4}{n}$
            [$\mysqrt{0}{0}{8}{n}$
                [...]
            ]
            [...]
        ]
        [$\mysqrt{0}{0}{4}{n}$
            [$\mysqrt{0}{0}{8}{n}$
                [...]
            ]
            [...]
        ]
        [...]
    ]
    [$\sqrt{n}$
        [$\mysqrt{0}{0}{4}{n}$
            [$\mysqrt{0}{0}{8}{n}$
                [...]
            ]
            [...]
        ]
        [$\mysqrt{0}{0}{4}{n}$
            [$\mysqrt{0}{0}{8}{n}$
                [...]
            ]
            [...]
        ]
        [...]
    ]
    [$\sqrt{n}$
        [$\mysqrt{0}{0}{4}{n}$
            [$\mysqrt{0}{0}{8}{n}$
                [...]
            ]
            [...]
        ]
        [$\mysqrt{0}{0}{4}{n}$
            [$\mysqrt{0}{0}{8}{n}$
                [..., name=a]
            ]
            [..., name=b]
        ]
        [..., name=c]
    ]
    [..., name=d]
  ]
    \node (p) [right=50pt of d] {$ = \sqrt{n}\sqrt{n} = n$};
    \node at (p |- c) {$ = \sqrt{n}\mysqrt{0}{0}{4}{n}\mysqrt{0}{0}{4}{n} = n$};
    \node at (p |- b) {$ = \sqrt{n}\mysqrt{0}{0}{4}{n}\mysqrt{0}{0}{8}{n}\mysqrt{0}{0}{8}{n} = n$};
    \node at (p |- a) {$ = n$};
\end{forest}
\end{center}

\begin{itemize}
    \item The amount of work at every level sums up to $n$
    \item Therefore the problem will be bond by $O(hn)$ with $h$ being the height of the tree
    \item To determine the height of the tree, assume we want to stop taking the square root of $n$ when $n$ gets smaller than 2:
    
    
    $$ \mysqrt{-4}{2}{2^h}{n} = 2 $$
    $$ n^{\frac{1}{2^h}} = 2 $$
    $$ \frac{1}{2^h}\log{}n = \log{}2 $$
    $$ \frac{1}{2^h}\log{}n = 1 $$
    $$ \log{}n = 2^h $$
    $$ h = \log{}\log{}n  $$
    
    \item Therefore $T(n) \in O(n\log{}\log{}n)$
    
\end{itemize}


\end{document}

